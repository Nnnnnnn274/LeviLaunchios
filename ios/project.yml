name: LeviLauncher
options:
  bundleIdPrefix: com.levimc
  deploymentTarget:
    iOS: "14.0"
  generateEmptyDirectories: true
  xcodeVersion: "15.0"

settings:
  base:
    SWIFT_VERSION: "5.9"
    TARGETED_DEVICE_FAMILY: "1,2"
    DEVELOPMENT_TEAM: ""
    CODE_SIGN_STYLE: Manual
    MARKETING_VERSION: "1.0.0"
    CURRENT_PROJECT_VERSION: "1"

targets:
  LeviLauncher:
    type: framework
    platform: iOS
    sources:
      - path: LeviLauncher
        type: group
        createIntermediateGroups: true
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.levimc.levilauncher
        MACH_O_TYPE: mh_dylib
        DYLIB_INSTALL_NAME: "@executable_path/LeviLauncher.dylib"
        EXECUTABLE_PREFIX: ""
        SWIFT_OBJC_BRIDGING_HEADER: LeviLauncher/Native/Bridge/Launcher-Bridging-Header.h
        SWIFT_ACTIVE_COMPILATION_CONDITIONS: LEVI_LAUNCHER
        CLANG_ENABLE_MODULES: YES
        SWIFT_FORCE_STATIC_LINK_STDLIB: NO
        SWIFT_FORCE_DYNAMIC_LINK_STDLIB: YES
        OTHER_LDFLAGS: ["$(inherited)", "-lz", "-lc++", "-lresolv", "-ObjC", "-all_load"]
        CLANG_CXX_LANGUAGE_STANDARD: "c++20"
        CLANG_CXX_LIBRARY: "libc++"
        ENABLE_BITCODE: NO
        IPHONEOS_DEPLOYMENT_TARGET: "14.0"
        SUPPORTS_MACCATALYST: NO
        SUPPORTS_MAC_DESIGNED_FOR_IPHONE_IPAD: NO
    info:
      path: LeviLauncher/Info.plist
    preBuildScripts:
      - name: "Create Config Files"
        script: |
          mkdir -p "$SRCROOT/LeviLauncher/Resources"
          if [ ! -f "$SRCROOT/LeviLauncher/Resources/CurseForge.plist" ]; then
            cat > "$SRCROOT/LeviLauncher/Resources/CurseForge.plist" << EOF
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0">
          <dict>
              <key>APIKey</key>
              <string>\$(CURSEFORGE_API_KEY)</string>
          </dict>
          </plist>
          EOF
          fi
