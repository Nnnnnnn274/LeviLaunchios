#ifndef ZOOMMOD_HPP
#define ZOOMMOD_HPP

namespace ZoomMod {

    void setEnabled(bool enabled);
    bool isEnabled();
    void setZoomLevel(float level);
    float getZoomLevel();
    void setAnimated(bool animated);
    bool isAnimated();

} // namespace ZoomMod

#endif // ZOOMMOD_HPP
